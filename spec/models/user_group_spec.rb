require 'spec_helper'

describe UserGroup do
   # it { should validate_presence_of :user_id }
   # it { should validate_presence_of :interest_group_id }

   # it { should belong_to :user }
   # it { should belong_to :interest_group }
end
